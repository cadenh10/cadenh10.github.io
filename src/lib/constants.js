// TODO(launch): replace with the real signed Mac installer URL once the DMG is
// hosted (e.g. a GitHub Release asset, S3/CDN link, or notarized download host).
// Until then this points at the local path so wiring stays consistent.
export const DOWNLOAD_URL = `${import.meta.env.BASE_URL}downloads/dialed-mac.dmg`;

export const SUPPORT_EMAIL = "workdialed@gmail.com";
